// Should not produce violations
const foo: ReadonlyArray<string> = [];

// Should produce violations
const foo: Array<string> = [];
           ~~~~~ [failure]

// Should warn on implicit mutable array type
const foo = [1, 2, 3]
      ~~~ [failure]

[failure]: Only ReadonlyArray allowed.
