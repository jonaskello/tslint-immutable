  // Should not produce failures
interface Foo {
  bar: string,
  zoo: number,
}
  // Should produce failures
interface Foo {
  bar: string;
  ~~~~~~~~~~~~ [failure]
}

[failure]: Use comma instead of semicolon in interfaces.
