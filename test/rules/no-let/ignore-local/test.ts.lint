var x = 0;

let x;
~~~~~~ [failure]

let x = 0;
~~~~~~~~~~ [failure]

for(let x = 0; x < 1;x++);
    ~~~~~~~~~ [failure]

function foo() {
  let x;
  let x = 0;
}

const foo = () => {
  let x;
  let x = 0;
}

class Foo {

  let x;
  ~~~~~~ [failure]
  let x = 0;
  ~~~~~~~~~~ [failure]

  foo() {
    let x;
    let x = 0;
  }

}

[failure]: Unexpected let, use const instead.
