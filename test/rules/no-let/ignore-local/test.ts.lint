var x = 0;

let x;
~~~~~~ [failure]

let x = 0;
~~~~~~~~~~ [failure]

for(let x = 0; x < 1;x++);
    ~~~~~~~~~ [failure]

function foo() {
  let x;
  let x = 0;
}

const foo = () => {
  let x;
  let x = 0;
}

class Foo {

  // Class members do not use let
  x;
  x = 0;

  foo() {
    let x;
    let x = 0;
  }

}

[failure]: Unexpected let, use const instead.
