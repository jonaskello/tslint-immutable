const x = {a: 1};
const y = {a: 1};

// Disallowed object mutation patterns

x.foo = 'bar';
~~~~~~~~~~~~~ [failure]

x['foo'] = 'bar';
~~~~~~~~~~~~~~~~ [failure]

delete x.foo;
~~~~~~~~~~~~ [failure]

x.a++;
~~~~~ [failure]

++x.a;
~~~~~ [failure]

// Allowed prefix

y.foo = 'bar';

y['foo'] = 'bar';

delete y.foo;

y.a++;

++y.a;

// Disallow Object.assign() on identifiers.

const value = { msg1: 'hello' };
const y = value;

const foo = Object.assign(value, { msg2: 'world' });
            ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [failure]

const foo = Object.assign(y, { msg2: 'world' });

[failure]: Modifying properties of existing object not allowed.
